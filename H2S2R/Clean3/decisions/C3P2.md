# Clean3 C3-P2：资产路径恢复→物理→PPO→外评

2026-09-13，Clean3 reviewer。接受C3-P1R2关闭，历史2/2启动保留，0物理样本/0外评分母，成功率N/A。这里显式放行新的有界C3-P2，不假称旧额度未消耗；本reviewer未启动实验。

## 独立定位与冻结

attempt02的entry_failure.json直接指向cfg.py:94把机器人USD拼接到v12源码根。实际V12训练入口另有bundle_root，资产与代码本来就不在同一个根。旧30/30文件及5/5依赖检查没有覆盖真实cfg消费的全部路径，不能当完整部署检查。reviewer此前未把住完整启动前检查，实施者按次数停止正确；这不是PCA表达能力或物理失败。

本次只读远端找到并sha256sum核验下列资产，与既有batch_delivery_01/input_identity.json冻结记录相同：

```
robot_usd=/home/kailang/experiments/baselines/pour17_baseline_bundle_20260829/pour17/world/vega_1p_sharpa_fixedtorso__RUNTIME_LOADED.usd
sha256=c2bba25aeb7b04b270df51d0c0efbe8eec95b499d8a7aef46834cefcfb71e347
format=USD crate 0.8.0
```

**冻结复用此现有机器人资产，禁止重造/替换机器人或碰撞。** 仅资产来自共享bundle，不加载Pour17任务、重置数据或策略。本次核验了路径与身份，未新做USD组合/58活动关节及碰撞运行认证；该验证列为下述CPU部署前置条件，不能凭文件名就写PASS。

## 工作步骤与条件放行

1. **一次性补齐部署检查（最多60分钟）。** Clean3 cfg明确区分code_root和robot_usd，以以上准确绝对路径读取共享资产（只读，不向共享目录补文件/软链接）。允许在Clean3 cfg及三入口传递明确robot_usd路径；同样的常规路径/interface支持改动无需逐函数审批。若制作独立资产副本，必须保留根层及全部依赖字节/SHA和相对解析关系，不允许flatten、重烘焙或替换碰撞作为路径修复。
2. 从实际cfg/入口消费字段生成单一必需资源清单，覆盖机器人、plate/sponge USD及所有组合/材质依赖、输入/侧车/PCA、双侧URDF、配置及实际导入模块；修复源码根/资产根混用。使用现有CPU可用USD工具进行stage打开和依赖解析，核对58个活动关节名称/限位、fixed torso、碰撞API及与既有记录的身份对应。不启SimulationApp、不构造物理世界；CPU工具缺失不得假装通过或为检查安装共享依赖。发现缺失依赖可按原身份定位同包资源，不能擅换资产。输出每项实际解析路径、SHA及结果，检查失败须在AppLauncher前非零退出。
3. **补监督器，不只补路径。** costs记录attempt02外部监控parser在dispatch后即失败，运行峰值未记录。用保存的资源输出做CPU回归（正常、多进程、空值/不完整输出），验证监控失败能被发现并停止自己的任务；保留异常落盘/无终态成功即incomplete机制。保持GPU_POLICY.md的10秒准入、映射、峰值预算和运行监测，不能用入口静态快照替代全过程监控。一次性检查三入口的解析/路径、导入、签名、记录和终态，不重复旧PCA拟合/参考生成。
4. **新增物理启动预算：最多2次，总45分钟，单次加载≤300秒。** CPU与资源条件全通过才使用C3-P2 physics01；只有支持性错误修复才可使用physics02，不能因真实物理/方法失败盲目重试。每次仍4env，沿用R2完整400步物理诊断、左右12子步、真实零动作首步/首秒目标与实测关节变化≤0.25rad、无深穿/超限、固定PD及FABRICS零动作各至少2/4持物1秒等门槛。不改变初态、PCA/校准、掌边界、ZYX、奖励或物理参数。
5. **通过即继续实际PPO和外评，不追加逐阶段审批。** 沿用尚未使用的256env、seed42、rollout16、minibatch4096，最多512轮/120分钟，一次正常训练启动，仅尚无优化器更新的支持失败可重启一次；不因低回报换22关节控制。按原定义初始随机模型/最后完整checkpoint各16episode外评，45分钟交付预算。没有有效分母N/A，真实失败计入分母。

C3-P2总墙钟预算270分钟（CPU60+物理45+PPO120+外评交付45），阶段/总预算先到者停止。历史C3-P1累计成本单独保留；PPO预算是原未执行pilot的继续，不是授权额外seed或扩训。资源不足按GPU_POLICY给具体缺口，共享允许且需持续监测，不无限排队。

## 结果与停止表

- 必需文件/依赖、签名或监控CPU检查失败：在CPU预算内自主支持修复，不启动Isaac探路；资产身份或物理构成变更则提前交reviewer。
- 真实物理门失败：交完整证据，不转22D、不改reward/初始化；仍未有PCA能力不足证据前不改方法。
- physics01为支持故障：预算内修复并重过完整CPU检查，再用唯一physics02；用完或达到时间停止，不再续试。
- 物理通过：同包直接训练、外评；0/N是结果，不自动扩训。
- 完成本包或明确阻塞/方法变更/预算到限：统一交付。禁止改其他线路或共享部署。

既有持续上传授权有效，独立Clean3源码/任务资产可传已确认服务器独立目录；如实际审批仍拒绝，保留具体理由，不绕过。新物理完整三视角视频本机交付；原始数值、PNG和checkpoint可留远端HDD，附路径、索引、SHA。0帧失败写N/A并交日志、失败阶段和所有启动成本，不称物理PASS/FAIL。

## 同步状态（2026-09-13 03:02:17 Asia/Shanghai）

本地支持修复未部署，完整CPU前置FAIL，本包新增启动0；旧R2的2/2不占本包2次额度。累计CPU成本需核账，局部0.5分钟不是全部。此处不追加预算。旧physics_gate_02命令禁止直接重用。
