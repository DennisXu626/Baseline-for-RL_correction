# 2026-09-13 GitHub交接同步（截止 2026-09-13 01:45:00 Asia/Shanghai (UTC+08:00)）

本次交接状态：V13/256 + 独立周期traceback禁用overlay，已实现、已部署，且一次GDB初始化成功；尚未完成训练集成或任何PPO更新。最新运行于2026-09-13 00:32:20–00:34:42北京时间（2026-09-12 16:32:20–16:34:42 UTC），环境在观测135.312秒返回，进程141.872秒，护栏在策略前退出。控制/更新/epoch/transitions均0，无checkpoint/normalizer，自主评测有效分母0，成功率未定义。左杯穿模不能由本次初始化判定解决，独立左杯track结论不在此更改。

当前入口：D:/UCBP/RL/Baselines/RL-Correction-H2S2R/tasks/h2s2r_pour17/README.md。逐文件完整上传表：D:/UCBP/reports/H2S2R_GITHUB_HANDOFF_20260913/UPLOAD_MANIFEST.md。

相较旧status的256 SIGSEGV状态，新增证据：实际dump_frame故障顶帧；GDB脚本修正；关闭周期dump的独立overlay实际加载，一次256初始化成功（INIT_RETURNED.json、launch_status.json及overlay_identity.json）。原V13科学源码仍冻结，训练入口没有宣告长期修复。新训练包待reviewer发布，本次不规划或授权实验。所有已关闭包不得从下面历史RELEASED/LIVE文字恢复。

GPU运营唯一权威D:/UCBP/GPU_POLICY.md；旧独占/固定GPU要求失效。仅更新Pour17/right入口，不更改左杯/Clean3权威结论。下方全部为历史记录，冲突时以本节、最新实际RESULTS和GPU_POLICY为准。

---


# H2S2R adapted baseline 当前情况与未关闭问题

> **GPU运营规则已于2026-09-12由用户替换：** [GPU_POLICY.md](../../../GPU_POLICY.md) 为双A6000的当前准入规则。GPU0/1均可选择，允许资源预算与持续监测支持的同卡/跨卡并行；已有compute进程不再单独阻止启动。通过准入后在本次子进程设置 `RL_ISAAC_NO_GUARD=1` 关闭旧机器级锁，记录实际GPU映射，不删除他人锁或中断他人任务。本文下方历史“独占/空闲卡/固定GPU”运营限制不再生效；历史运行事实、科学参数、track所有权及启动额度不变。

右手最新执行（2026-09-12）：隔离V13 256env适配的28项CPU、32项身份与GPU1资源门PASS，但第1次真实训练32.311839秒发生native SIGSEGV/-11，环境未返回，0控制/更新。按包停止且第2次不可用；左右物理风险、穿模与自主评测均NOT_TESTED，无模型/视频，未运行不记0%。证据：`H2S2R_TRAINING_PILOT_20260912/right_training_256_01/RESULTS.md`。

当前最终状态（2026-09-12 13:21:27UTC，imple1右手）：resume02 STOP_NATIVE_CRASH。原最后第2次启动已使用（2/2）；GPU1、1024env、seed42，进程122.233336s以SIGSEGV/-11退出，非超时。环境未返回，0采样/更新，无checkpoint/视频；左杯未观察，终评未运行，不能记成功率0%。累计训练进程609.315635s。21项CPU与32项部署身份PASS不等于物理PASS。停止本恢复，无第3次；完整证据见H2S2R_TRAINING_PILOT_20260912/right_training_result_01/resume02/RESULTS.md。以下历史放行/运行中记录已被本状态覆盖，不影响其他并行任务。

更新：2026-09-12。本文件是状态汇报，不是新的实验授权。

**最新恢复：right_training_result_01/resume01 / STOP_INITIALIZATION_TIMEOUT。** 按新分层存储裁定完成容量门、26项新增CPU与32项身份，实际启动GPU1/1024env/seed42训练入口；初始化未返回，在480秒硬截止结束（额外清理7.082秒）。0controls、0优化更新、0epoch，无checkpoint/终评。左手杯本轮未观察，成功率未定义；不是0%学习失败。栈停在initialize_physics，经SimulationContext.reset调用，未断言唯一原因。启动1/2已用但第二次恢复资格未满足，停止交reviewer。详见 本轮RESULTS（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/right_training_result_01/resume01/RESULTS.md`）；下面旧存储阻塞与batch结论作为历史保留。

**最新训练包：right_training_result_01 / BLOCKED_INFRA_LOCAL_STORAGE。** reviewer已放行训练，但本次启动前资源门失败：GPU1可用，D盘74.64GB，单独终评原始帧+20%需107.86GB（尚不含训练raw等）。未改支持/核心、未部署、未运行探针/物理/PPO/终评；训练更新0、checkpoint无。左手抓杯历史问题仍未获本轮观察；右手未学习，评测成功率未定义。新启动额度未使用；旧batch2/2不变。证据：RESULTS（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/right_training_result_01/RESULTS.md`）。停止等待存储条件与reviewer恢复裁定，不轮询或排队。

**最新交付覆盖下方历史状态：batch_delivery_01 / DELIVERED_PENDING_REVIEWER。** 统一三入口录像/记录支持完成，原42/新增43项CPU及32项身份通过；本包第1次0controls支持失败保留，第2次16×256controls完成。SUPPORT/RUNTIME/MEDIA PASS，PHYSICS执行者有限样本筛查PASS、待reviewer裁定；完整本机三视角各256帧、16环境控制轨迹、env0全部3072子步已交付，912文件SHA通过。未观察到明显持续深穿，但不是全环境零穿透认证。两次物理进程合计904.768秒，额度2/2耗尽。PPO/终评均未启动，有效评测分母0、成功率未定义，不是0%。黑桌、hold0及核心方法保持。下一动作仅reviewer统一审阅，不自动训练。详见 [RESULTS](../evidence/batch/RESULTS.md)。下方旧short1/1及391.167秒录像不足结论作为历史保留，不改写为PASS。

最新执行安排：相机/短物理工作包已完成。reviewer已裁定进入实际右手训练结果包，优先取得PPO更新、checkpoint及自主终评；左手抓杯穿模风险并入同一训练进程初始化/首轮检查，不再独立重复右侧验证。当前奖励先保留，抓取奖励适配留作根据真实训练证据决定的明确变体。详见 [当前计划](H2S2R_RIGHT_TRAINING_RESULT_PACKAGE_20260912.md)。本次只审阅和写计划，未启动训练。

## 当前结论

**最新实测与恢复：** imple1的resume01启动了1024环境训练入口，但在PhysX从USD加载物理场景时达到480s初始化截止，仍0采样/0更新/无checkpoint，左杯未进入观察。reviewer已核实具体调用并裁定 最后一次规模初始化恢复（外部/历史定位：`D:/UCBP/reports/H2S2R_1024_INITIALIZATION_REVIEW_20260912.md`）：不改方法/物理，原第2次启动的train初始化上限1800s，累计6h扣除旧487.082299s；成功直接学习和评测，再失败不追加第3次或继续仅延时。新恢复未执行，不能写成已经训练。原有并行左杯问题继续OPEN。用户只需本机视频，完整原始数据/checkpoint可远端留存且可核验。

**最新纠正与裁定：** 用户所指穿模在左手抓杯，前包主要筛查右瓶，不能认证左侧已修复。batch_delivery_01已完成清晰正常策略短样本及运行链检查，reviewer放行[实际训练结果工作包](H2S2R_RIGHT_TRAINING_RESULT_PACKAGE_20260912.md)，不再独立重复右侧短验证；左杯风险并入同一训练进程首轮，重大物理问题才阻止更新。相机跟随实测瓶掌中点造成背景晃动已由源码和元数据确认，非训练前必须修复项。当前仍无修正版训练/评测结果，新包尚未执行。具体证据和优先级见放行审阅（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_RELEASE_REVIEW_20260912.md`）。下方旧阶段叙述保留历史边界，新执行入口以上述为准。

我们已有保留 H2S2R 的 PPO/LSTM + FABRICS 框架、适配 UCBP 机器人和 Pour17 场景的实现，也修正了几处确定的控制实现错误。最新 V12 唯一短验证已用正常随机初始化官方策略完成16环境×256 controls，运行391.167秒；真实限位读回、1/12/12调用、hold0、左固定命令、finite及done/reset检查通过并测得接触。但录像top偏离瓶手区域、front首帧全黑且env0三视角对应不足，物理有效性为INCONCLUSIVE，而非PASS。未启动PPO或终评；短额度1/1已使用，不自行重试。

所以它仍是**待完成物理验证和训练的 adapted baseline**，不是可以用于正式双手 Pour17 成功率比较的成品。最近许多停止发生在启动、记录和验收脚本上，消耗了工程时间，却没有产生新的抓举失败样本。

## 当前 baseline 的构成

| 项目 | 实际情况 |
|---|---|
| 最终用途 | UCBP 双手 Pour17 的 H2S2R adapted 比较基线，不承担完整原生长周期复现 |
| 当前阶段 | 右手抓瓶 pilot，左侧保持固定命令；尚非完整双手倒水任务 |
| 上层 | 官方循环 PPO/LSTM 路径；近期修正版尚无训练产出的策略 |
| 动作 | 11 维：6 维掌部目标 + 5 维手部任务目标 |
| 下层 | FABRICS 生成右侧 7 个臂关节 + 22 个手关节命令，再交给物理执行器 |
| 手部表达 | 基于 Sharpa 关节数据的五维投影，不是直接复制 MANO 的完整手形到 22 个关节 |
| 适配 | Vega/Sharpa 模型、关节与任务映射、UCBP 场景、当前 pilot 的近抓取初始化等，须在比较中披露 |
| 冻结 | 当前要求 hold=0，不启用历史 15 步物品位姿/速度覆盖；不等于穿模已解决 |
| 桌面 | 当前 V12 部署源码为黑桌；颜色只用于版本辨识，不能证明物理有效 |
| 评价阶段 | 当前修正版未训练、未终评；右手验收仍为 PENDING |

实际执行根目录：远端 `/home/kailang/experiments/baselines/rl_correction_h2s2r_adapter_v12_training_pilot_20260912`。本地 `D:\UCBP\RL\Baselines\RL-Correction-H2S2R` 是早期副本，不能代替此执行版本。已保存审计源码：source_after（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/source_after`）。

## 仍然存在的问题

### 1. 验收脚本误判已修正；实际限位读回与控制计数已通过

限位补丁把左右都包装为同一个类，但左侧分支继续调用原实现。旧验证却把“左侧也是这个类”视为修复泄漏，因此拒绝了预期实现。

这是已经确认的检查错误，不是新证明的左手控制错误。reviewer 的真实方法体 CPU 验证后，implementation 已完成三个入口检查替换与观测集成并部署；本机/目标CPU各42项PASS。本次恢复未重做准备或探针，31项部署身份再次PASS，实际16×29两侧限位读回也PASS。保留冻结补丁/controller/env/参数；本次只运行与记录，没有修改执行源码。

本次09:38:41UTC资源快照GPU1无compute进程、48,062MiB free，满足资源门；GPU0仍占用。唯一短验证PID2432691于09:41:24启动、09:47:56结束，256control/3072积分与提交、左advance0、活动物品写入0。上一轮资源阻塞记录仍保留，本次未并跑、未轮询，也未新增CPU或初始化/导入probe。裁定（外部/历史定位：`D:/UCBP/reports/H2S2R_V12_LEFT_DELEGATION_REVIEW_20260912.md`）；最新结果（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/physics_resume_01/RESULTS.md`）。

### 2. 穿模仍未验证：完整运动记录的三视角覆盖不足

用户观察到去掉冻结后仍穿模，而初代没有。现有证据不能把全部现象归因于冻结；关闭覆盖写入只是去除一个已知风险路径。成功初始化、静态碰撞参数检查都不证明运动接触正常。

本次已完成完整短运行，三段MP4各256帧，全部16环境控制原始轨迹及env0全部3072子步已保存。env0接触力最大1.0342N，但top第1/65帧均未覆盖瓶手区域，front/side出现多个环境，无法可靠完成env0穿模排除。判INCONCLUSIVE_VISUAL_EVIDENCE，停止训练依赖分支；既不是随机策略没抓起导致失败，也不因有接触力就判无穿模。不改镜头或补跑，等待明确裁定。结果与样本（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/physics_resume_01/RESULTS.md`）。

### 3. FABRICS 的完整手形不利于抓瓶，但不能直接定性为新 bug

某帧成功参照拇指末节约 51.2°，FABRICS 命令约 15.5°，实际执行约 15.5°。差异已存在于命令，不是主要因为正确角度下发后电机没跟上。

同一帧五维任务误差仅约 0.000185 rad，完整手形仍相差很大。五维目标不能唯一决定全部 22 个关节，其余方向由默认姿态、限位等项共同选择。证据支持这解释了不利拇指姿态的形成机制，但没有证明求解器算错，也没有证明所有合法策略都无法抓瓶。

因此不擅自扩大 PCA、增加拇指监督或注入成功源完整手形。这些属于方法改动，须单独决策。拇指手形审阅与原始数据索引（外部/历史定位：`D:/UCBP/reports/H2S2R_RIGHT_THUMB_SHAPE_REVIEW_20260912.md`）。

### 4. 修正版自主学习能力未知

最近两段完整失败视频是诊断回放：一条使用完整 FABRICS，另一条用成功源臂命令加 FABRICS 手部输出。都没有运行训练后的 PPO；诊断目标也不能等同于正常归一化策略动作。第二条腕部复现超过预设误差门，因此后续干预没有进行。

这两个不同条件各一次失败，证明修复没有自动恢复该源轨迹抓举，不能合成正式成功率。近期 V12 训练为 0 秒、0 epoch、0 transition；终评未运行，不是 0/16。历史 Run01–11 确实包含训练，但其代码/配置不同，不代表当前修正版结果。

### 5. 旧超时根因和训练规模的稳定性仍有未知

旧 30 分钟初始化超时没有确定根因；后续定位全进程约 38.03 秒、环境构造约 19.29 秒正常完成。已证明当前代码能够初始化，不需要无限重复初始化审计；也不能据此归咎于 GPU 编号或模型服务容量。

本次完成16环境初始化及256 controls，但1024环境训练规模的显存、吞吐和长时稳定性尚无本轮实测。本次GPU1资源门已通过；当前停止原因是视觉证据不足，不是旧资源阻塞。初始化定位结果（外部/历史定位：`D:/UCBP/reports/H2S2R_TRAINING_PILOT_20260912/init_trace/RESULTS.md`）。

### 6. 左侧、上游映射与完整任务尚未全面验收

右手零内缩修复未扩展到左手。左侧保持原行为，包含窄关节内缩的既有限制；右手 pilot 的进度不能被描述成双手系统已修好。

MANO/Sharpa 局部几何不匹配、关节顶限位和整体坐标姿态问题不能合并成一个已证实根因。已核验关节顺序、投影与命令链，不等于整个上游重定向质量已验证；也没有新证据证明当前统一存在整体坐标 bug。它们目前不构成启动无界调查的前置条件。

完整双手配合、倒水、放置及正式评测仍未完成。

## 接手前后真正发生的进展

| 问题或起点 | 当前变化 | 结论边界 |
|---|---|---|
| Sharpa 固定 ±10° 内缩造成部分限位区间矛盾 | 右手补丁保留物理手限位，改善手形；此项在接手起点已修复 | 尚未恢复抓举，左侧未修复 |
| FABRICS 调度偏差 | 修正为每物理子步推进，并完成两条修正后诊断轨迹 | 调度修复不足以恢复抓举 |
| 普通训练入口未继承完整修复、每 control 出现 13 次推进 | V12 安装右手补丁，实际正常策略256 controls验证一次目标更新、12次积分/提交 | 控制计数通过，穿模有效性未完成验收 |
| 五维报告误差差异、过严浮点容差 | 已解释测量对象差异及 float32 舍入，旧 FAIL 保留 | 不再将其当成未解控制 bug |
| 只能笼统怀疑 FABRICS 或电机 | 原始数据定位到完整手形命令选择的问题 | 未证明方法必然失败 |
| 启动导入、退出前保存等脚本错误 | 已修正相关支持并取得完整初始化证据 | 不证明穿模修好或训练开始 |
| 最新左侧类检查误判 | 三入口支持已修正部署，历史CPU各42项PASS；本次真实两侧限位读回及左命令隔离PASS | 接触力已记录，但视觉证据不足以排除穿模 |

新增的能力证据是：完成两条修正后诊断、解释部分失败机制，以及V12正常策略短运行完成并通过限位和控制计数检查。**尚未新增修正版自主策略能抓瓶的证据。** 大量近期工作确实用于让启动和验收可靠，不能包装成任务性能进步。

## 已选下一步与论文口径

检查修正与观测支持已完成，不再重复。唯一短物理额度已消耗；由于视觉覆盖不足，物理有效性未获PASS，已停止训练与终评分支。不自行修镜头或追加样本，后续须明确裁定。随机策略未抓起不是本次停止依据。

按恢复计划（外部/历史定位：`D:/UCBP/reports/H2S2R_V12_RESUME_PHYSICS_AND_PILOT_20260912.md`）及验收裁定（外部/历史定位：`D:/UCBP/reports/H2S2R_V12_LEFT_DELEGATION_REVIEW_20260912.md`）执行停止分支。短进程391.167秒，未超时；本机已交付完整三视角MP4、全部控制原始记录及env0子步轨迹、启动身份/日志与成本。训练和16episode终评均未执行，本文不追加授权。

可用于当前阶段汇报的文字：

> 我们构建了面向 UCBP 机器人及 Pour17 场景的 H2S2R 适配实现，保留循环 PPO 与 FABRICS 控制结构，并修正关节限位及调度的实现偏差。两次修正后的源目标诊断回放仍未实现抓举，数据表明五维手部目标的准确跟踪不足以保证复现成功参照的完整手形。当前训练版本完成了16环境正常随机策略短运行，限位读回及控制计数通过，但三视角覆盖不足以排除穿模，未进入训练和独立终评，因此不报告正式成功率。

将来若固定协议评测得到 0/N，应归属于记录了适配、预算和条件的这个实现。当前不能写“同行方法成功率 0%”，也不能写“关掉冻结后穿模已解决”。

## 证据时点

最新executor结果为 `physics_resume_01/RESULTS.md` 的STOP_SHORT_VISUAL_INCONCLUSIVE：2026-09-12 09:38:41UTC单次资源检查，09:41–09:47UTC运行。本机完整归档SHA256为1697ac5edcdc27f717cd7f6cc743b7a24c9ad29fd22a19671fc8030046dc8733，73文件逐项核验。历史CPU与支持部署PASS保留，本次未重做测试/探针；旧成本与失败保留在prior_interface_stop及resume02/prior_state等记录。当前实现运行检查PASS不等于物理有效性或RIGHT Gate B通过，终评有效分母0、成功数未评测。交接旧Latest段落不得覆盖本最终状态。


