# A6000 GPU0/1 共享准入规则

状态：用户于2026-09-12明确授权，取代本项目所有旧的“有其他compute进程就不准用”“必须独占GPU”“固定只能GPU1”等运营限制。适用于该双A6000服务器上的UCBP任务，包括Pour17/right、left/cup、Clean3/plate和其他baseline；不改变各track所有权、科学配置、剩余启动次数或已关闭状态。

## 可以直接使用的条件

GPU0、GPU1均可选。现有计算进程只是资源信息，不是拒绝条件；同卡并跑和两卡分别运行都允许。执行者完成以下资源判断即可在原工作包授权内启动，不需要再请求“共享GPU”的许可。

1. 启动前连续采样约10秒，记录两卡UUID、显存total/used/free、GPU利用率，以及主机MemAvailable。核对现有计算进程的公开PID/占用即可，不读取他人环境、内存或私有输出。
2. 显存准入：可用显存必须覆盖本任务**初始化/稳态/渲染峰值中的最大预算×1.2，再加4GiB公共余量**。不能把进程刚创建时的占用当完整训练峰值。峰值来源优先同配置实测，否则给出保守估计并记录不确定性；没有可信预算不能宣称资源足够，但不要求另开物理实验，利用已授权启动过程计量。
3. 算力准入默认采用10秒样本平均利用率≤80%、最大≤90%，并结合本任务预计负载、同时初始化情况判断。选显存有足够余量且负载更低的一张，不固定优先0或1。均满足时任选，记录映射；均不足报告具体显存/负载缺口，禁止仅写“有其他进程”。这些是可调运营阈值，不是物理或方法参数。
4. 主机RAM、CPU、磁盘和初始化峰值也不能被忽略。沿用工作包已有主机保护；缺省MemAvailable不得低于8GiB。多任务不能同时把同一空闲显存重复算成自己独占余量：各执行者记录GPU UUID、预计峰值、角色和当前初始化状态；发起进程前立即复查资源。存在同一GPU刚启动而尚未计入峰值的任务时，应把它的剩余预算扣除，或选另一张卡。不能声称一次快照消除了竞争。

## 运行中如何处理

- 复用任务已有外部监督器，每10秒左右记录显存/利用率/主机RAM/阶段；初始化和训练都计入。不能仅放行后不管。
- 显存空余低于2GiB连续3个样本、主机MemAvailable低于8GiB连续3个样本，或出现明确OOM/驱动错误，保存并退出**本任务**，不能终止其他任务。
- 有共享进程时，持续≥60秒利用率≥95%应视为资源竞争告警：检查本任务和可获的同项目心跳/阶段进度，在安全边界让本任务停止占用或退出并保存；不杀、不暂停他人作业。单独一个任务正常把GPU用满不是错误。不能宣称仅凭总GPU利用率就精确知道各任务吞吐损失。
- 超过阈值不允许改batch、环境数量、奖励或物理来悄悄降负载；是否改变科学配置仍归所属reviewer。任务自己的截止与异常规则保留，资源共享不赠送重试额度。
- 允许一次准入窗口及运行监督，不新增无限等卡轮询/排队。若原用户另有有界等待授权可沿用。

## 旧代码锁必须如何处理

已只读核实实际V12 `rl_rebuild/utils/gpu_guard.py` 默认持有机器级flock，并通过同目录pause.request串行化训练/录像。它来自旧5090单卡机器的历史保护逻辑，不是A6000驱动要求。旧源文档已有关闭开关。

通过上述共享资源准入后，对**本次子进程**在AppLauncher、gpu_guard import之前设置：

```text
CUDA_VISIBLE_DEVICES=<选定物理GPU索引或UUID>
RL_ISAAC_NO_GUARD=1
```

程序内部仍按所选映射使用cuda:0。同时记录实际物理GPU UUID，不把进程内cuda:0误写成物理GPU0。允许相对旧manifest只改变这些已授权运营字段并写operational_diff；不可因旧文件冻结GPU1而拒绝GPU0。

本开关关闭的是旧机器级锁和旧全局pause协议；不能把它误称为完成资源检查。必须结合本文件的准入、监督、任务截止。不要删除他人的isaac.lock/pause.request，不要改全局shell配置，不要用“每卡一个锁”继续制造同卡独占。已运行任务不因本文件自动被中断或重启。

各执行者可在其所属launcher/资源预检支持中加入上述环境与监测，更新支持SHA和CPU反例；该运营修正已由用户授权，不再请求小项批准。核心/FABRICS/物理源码和历史冻结副本不需改。历史manifest、RESULTS及失败原因原样保留；旧source snapshot中的锁实现保留用于复现，新的启动配置覆盖它。

## 边界

“负载和显存足够”不能数学保证并行永不出错；本规则允许有监测和退出边界的共享，不承诺零干扰。也不能把旧native crash归咎于共享或声称本改动修复了SIGSEGV。本次只改变运营准入，未启动实验，未改变任何track的方法、剩余额度或关闭决定。
